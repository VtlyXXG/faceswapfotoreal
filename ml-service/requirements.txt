# Python 3.11
# Тяжёлого локального инференса больше нет: замена лица выполняется на fal.ai.
# Локально остаются только построение маски лица и работа с изображениями.

# --- Web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Облачный инференс: генеративный инпейнт с переносом идентичности ---
# fal-client НЕ ставится по умолчанию. Путь через fal выключен настройкой
# (ML_FAL_ENABLED=false), личность переносится своим Flux + PuLID на GPU-боксе,
# а пересадка головы считается локально. Держать в обязательных зависимостях
# клиент чужого платного сервиса значит требовать его от всякого, кто просто
# склонировал репозиторий.
#
# Нужен старый путь — поставьте вручную вместе с ключом:
#     pip install fal-client==0.5.6   # и ML_FAL_ENABLED=true, FAL_KEY=...
# Без него включённый путь честно скажет, чего не хватает, а выключенный не
# заметит отсутствия вовсе: импорт ленивый, внутри fal_api.client().
requests==2.32.3

# --- Маска головы на шаблоне и обработка изображений ---
# mediapipe несёт и сетку лица, и ImageSegmenter для семантической разметки
# (лицо / волосы / кожа тела / одежда). Отдельного пакета для разметки не нужно,
# нужен только файл весов ~16 МБ — см. app/pipelines/parsing.py и README.
mediapipe==0.10.14
opencv-python-headless==4.10.0.84
numpy==1.26.4

# rembg здесь больше нет: он вырезал голову заказчика для аппликации, а
# аппликации в пайплайне не осталось — голова рисуется генеративно внутри
# маски шаблона. Вместе с ним ушли onnxruntime, pymatting, numba и ~350 МБ
# весов u2net.

# --- Прочее ---
python-dotenv==1.0.1
