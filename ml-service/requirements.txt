# Python 3.11
# Тяжёлого локального инференса больше нет: замена лица выполняется на fal.ai.
# Локально остаются только построение маски лица и работа с изображениями.

# --- Web ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Облачный инференс: Flux inpainting + IP-Adapter ---
fal-client==0.5.6
requests==2.32.3

# --- Маска лица, разметка человека и обработка изображений ---
# mediapipe несёт и сетку лица, и ImageSegmenter для семантической разметки
# (лицо / волосы / кожа тела / одежда). Отдельного пакета для разметки не нужно,
# нужен только файл весов ~16 МБ — см. app/pipelines/parsing.py и README.
mediapipe==0.10.14
opencv-python-headless==4.10.0.84
numpy==1.26.4

# --- Сегментация головы: силуэт волос, которого нет в сетке лица ---
# Тянет за собой onnxruntime, pymatting и numba. Веса (~176 МБ на модель)
# качаются при первом запуске в ~/.u2net — в Docker они кладутся на этапе
# сборки, см. Dockerfile.
rembg==2.0.59

# --- Прочее ---
python-dotenv==1.0.1
